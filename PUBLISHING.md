# Publishing notes

The `main` branch contains the public source release.

For release artifacts, use the source tree verified by GitHub Actions and the SHA-256 manifest.

Current public version: **v1.3.2**.

The public build intentionally supports Claude Desktop only. ChatGPT Desktop / Codex remains a roadmap item until a separate target-specific integrity and rollback adapter is verified.
