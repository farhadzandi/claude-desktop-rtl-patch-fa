{
  "name": "claude-desktop-rtl-patch-fa",
  "version": "1.3.2",
  "description": "Persian-first RTL patch for Claude Desktop on Windows with hardened ACL restore, secure rollback, audit and verification tools.",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node --test",
    "build:py": "python3 tools/build-payload.py"
  }
}
